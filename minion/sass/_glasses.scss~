.glasses {
    width: 180px;
}

.band,
.band-left:before,
.band-right:before {
    position: relative;
    display: block;
    width: 17px;
    height: 10px;
    background: #222;
    border-radius: 3px;
    box-shadow: 0 1px 5px 0px #222;
}
// 左边的带子
.band-left {
    top: 96px;
    left: -5px;
    transform: rotate(-5deg);

    &:before {
        content: "";
        display: block;
        top: 10px;
        background: #333;
    }
}
// 右边的带子
.band-right {
    top: 89px;
    left: 168px;
    transform: rotate(5deg);

    &:before {
        content: "";
        display: block;
        top: 10px;
        background: #333;
    }
}
