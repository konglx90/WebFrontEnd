import React, { PureComponent } from 'react';
import './styles.less';

const classPrefix = 'backtotop';

class BackToTop extends PureComponent<> {
  render() {
    return (
      <div className={`${classPrefix}`}>
        backtotop
      </div>
    );
  }
}

export default BackToTop;
