// @flow

// function foo(x: ?number): string {
//   if (x) {
//     return x.toSting();
//   }
//   return "default string";
// }

// foo(2);

function square(n: number): number {
  return n * n;
}

const z = square(2);
